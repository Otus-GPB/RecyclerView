<resources>
    <!-- Base application theme. -->
    <style name="Theme.RecyclerView" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="textColor1">@color/mineShaft</item>
        <item name="textColor2">@color/abbey</item>
        <item name="textColor3">@color/osloGray</item>
        <item name="textColor4">@color/white</item>
    </style>
</resources>